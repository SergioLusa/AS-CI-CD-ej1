name: Workflow ej1

on: push

jobs:

  hola:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Instalar pytest
        run: pip install pytest
      - name: Probar código
        run: pytest test.py
